import { formatDate } from '../../shared/utils/formatDate';

const currency = new Intl.NumberFormat('es-DO', { style: 'currency', currency: 'DOP' });

export function RecentTransactionsTable({ transactions }) {
  return <section className="overflow-hidden rounded-xl border border-slate-800 bg-slate-900 shadow-lg"><div className="border-b border-slate-800 px-5 py-4"><h2 className="font-semibold">Últimas transacciones</h2></div><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-800/60 text-slate-400"><tr><th className="px-5 py-3">Descripción</th><th className="px-5 py-3">Categoría</th><th className="px-5 py-3">Fecha</th><th className="px-5 py-3 text-right">Monto</th></tr></thead><tbody>{transactions.length ? transactions.slice(0, 5).map((transaction) => <tr key={transaction.id} className="border-t border-slate-800"><td className="px-5 py-3 font-medium">{transaction.description || 'Sin descripción'}</td><td className="px-5 py-3 text-slate-400">{transaction.category}</td><td className="px-5 py-3 text-slate-400">{formatDate(transaction.createdAt ?? transaction.created_at)}</td><td className={`px-5 py-3 text-right font-semibold ${transaction.type === 'income' ? 'text-emerald-400' : 'text-rose-400'}`}>{transaction.type === 'income' ? '+' : '-'}{currency.format(transaction.amount)}</td></tr>) : <tr><td colSpan="4" className="px-5 py-8 text-center text-slate-400">Aún no hay movimientos registrados.</td></tr>}</tbody></table></div></section>;
}
